 let config = require('./config.js');
 const mysql = require('mysql');
 var express = require('express');
 var expressAppName = express();

// create a connection variable with the required details

//connection to DB and display data from DB
con.connect(function (err) {
    if (err) throw err;
    app.get('/database-show', function (req, res) {
        con.query("SELECT id,name FROM identity", function (err, result, fields) {
            res.render('main', {
                layout: 'dataDB',
                dataID: result
            })
            if (err) throw err;
        });
    })
    app.get('/database-insert',function(req,res){
        if (err) throw err;
        con.query("INSERT INTO identity(name) VALUES ('Logo-insert')", function (err, result) {
            if (err) throw err;
            console.log("j'ai insert");
          });
    })
    app.get('/database-delete',function(req,res){
        if (err) throw err;
        con.query("DELETE FROM identity WHERE name ='Logo-insert'", function (err, result) {
            if (err) throw err;
            console.log("j'ai delete");
          });
    })
});

//GOOGLE AUTH
            var profile = googleUser.getBasicProfile();
            console.log('ID: ' + profile.getId()); // Do not send to your backend! Use an ID token instead.
            console.log('Name: ' + profile.getName());
            console.log('Image URL: ' + profile.getImageUrl());
            console.log('Email: ' + profile.getEmail()); // This is null if the 'email' scope is not present.
